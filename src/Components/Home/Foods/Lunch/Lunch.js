import React from "react";

const Lunch = ({ lunch }) => {
    const { picture, title, description, price } = lunch;
    return (
        <div>
            <h1>Lunch</h1>
        </div>
    );
};

export default Lunch;
